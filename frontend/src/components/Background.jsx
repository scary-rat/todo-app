import React from "react";

const Background = () => {
    return (
        <div className="absolute top-[50%] left-[50%] -translate-x-[50%] -translate-y-[50%]">
            <h1 className="heading text-zinc-600 text-[11vw]">Notes.</h1>
        </div>
    );
};

export default Background;
